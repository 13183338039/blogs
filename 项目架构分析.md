# VueBlog 后端项目详细架构分析

## 项目概述

这是一个基于 **Spring Boot 2.2.6** 开发的博客系统后端项目，采用前后端分离架构，使用 **JWT + Shiro** 进行身份认证和权限管理，**MyBatis Plus** 作为 ORM 框架，**Redis** 用于会话管理和缓存。

---

## 项目架构

### 架构分层
```
Controller 层 (控制器) 
    ↓
Service 层 (业务逻辑)
    ↓
Mapper 层 (数据访问)
    ↓
Entity 层 (实体类)
```

---

## 目录结构详解

### 1. 主启动类

#### `VueblogApplication.java`
- **作用**: Spring Boot 应用入口
- **功能**: 启动整个应用，自动配置 Spring 容器

---

### 2. Entity 层（实体类）

#### `entity/User.java` - 用户实体
```java
@TableName("users")  // 对应数据库表 users
```
**字段说明**:
- `id`: 主键，自增
- `username`: 用户名（必填）
- `avatar`: 头像URL
- `email`: 邮箱（必填，格式验证）
- `password`: 密码（MD5加密存储）
- `status`: 状态（-1=锁定，0=正常）
- `created`: 创建时间
- `lastLogin`: 最后登录时间

**验证注解**:
- `@NotBlank`: 非空验证
- `@Email`: 邮箱格式验证

#### `entity/Blog.java` - 博客实体
```java
@TableName("blog_articles")  // 对应数据库表 blog_articles
```
**字段说明**:
- `id`: 主键，自增
- `userId`: 作者ID（外键关联用户）
- `title`: 标题（必填）
- `description`: 摘要（必填）
- `content`: 内容（必填，支持Markdown）
- `created`: 创建时间（JSON格式：yyyy-MM-dd）
- `status`: 状态（0=正常，1=删除等）

---

### 3. Controller 层（控制器）

#### `controller/AccountController.java` - 账户控制器
**功能**: 处理用户登录、登出等账户相关操作

**接口列表**:
1. `GET /test` - 测试接口
   - 返回 "hello world"

2. `POST /login` - 用户登录
   - **请求体**: `LoginDto` (username, password)
   - **流程**:
     - 根据用户名查询用户
     - 验证密码（MD5加密对比）
     - 生成JWT Token
     - 将Token放入响应头 `Authorization`
     - 返回用户信息（id, username, avatar, email）
   - **响应**: 用户基本信息 + JWT Token

3. `GET /logout` - 用户登出
   - **权限**: 需要认证（`@RequiresAuthentication`）
   - **功能**: 清除Shiro会话

#### `controller/BlogController.java` - 博客控制器
**功能**: 处理博客文章的CRUD操作

**接口列表**:
1. `GET /blogs` - 获取博客列表（分页）
   - **参数**: `currentPage`（默认1）
   - **功能**: 分页查询，每页5条，按创建时间倒序
   - **权限**: 公开访问

2. `GET /blog/{id}` - 获取博客详情
   - **参数**: 博客ID
   - **功能**: 根据ID查询单篇博客
   - **权限**: 公开访问

3. `POST /blog/edit` - 编辑/创建博客
   - **权限**: 需要认证
   - **功能**: 
     - 如果 `blog.id` 存在：更新博客（只能编辑自己的文章）
     - 如果 `blog.id` 不存在：创建新博客
   - **权限控制**: 编辑时验证 `userId` 必须等于当前登录用户ID
   - **自动设置**: 创建时间、状态、用户ID

#### `controller/UserController.java` - 用户控制器
**功能**: 处理用户信息相关操作

**接口列表**:
1. `GET /user/index` - 获取用户信息
   - **权限**: 需要认证
   - **功能**: 获取ID为1的用户信息（示例接口）

2. `POST /user/save` - 保存用户
   - **功能**: 保存用户信息（目前仅返回，未实际保存）

---

### 4. Service 层（业务逻辑层）

#### `service/UserService.java` - 用户服务接口
- 继承 `IService<User>`，提供MyBatis Plus的基础CRUD方法

#### `service/BlogService.java` - 博客服务接口
- 继承 `IService<Blog>`，提供MyBatis Plus的基础CRUD方法

#### `service/impl/UserServiceImpl.java` - 用户服务实现
- 实现 `UserService` 接口
- 继承 `ServiceImpl<UserMapper, User>`
- 目前使用MyBatis Plus提供的默认实现

#### `service/impl/BlogServiceImpl.java` - 博客服务实现
- 实现 `BlogService` 接口
- 继承 `ServiceImpl<BlogMapper, Blog>`
- 目前使用MyBatis Plus提供的默认实现

---

### 5. Mapper 层（数据访问层）

#### `mapper/UserMapper.java` - 用户Mapper
- 继承 `BaseMapper<User>`
- 提供基础CRUD方法（MyBatis Plus自动生成）

#### `mapper/BlogMapper.java` - 博客Mapper
- 继承 `BaseMapper<Blog>`
- 提供基础CRUD方法（MyBatis Plus自动生成）

#### `resources/mapper/UserMapper.xml` - 用户Mapper XML
- 目前为空，可扩展自定义SQL

#### `resources/mapper/BlogMapper.xml` - 博客Mapper XML
- 目前为空，可扩展自定义SQL

---

### 6. Config 层（配置类）

#### `config/ShiroConfig.java` - Shiro安全配置
**作用**: 配置Apache Shiro安全框架

**核心配置**:
1. **SessionManager**: 使用Redis存储会话
   - `RedisSessionDAO`: Redis会话持久化

2. **SecurityManager**: 安全管理器
   - 注入 `AccountRealm`（认证域）
   - 注入 `SessionManager`（会话管理器）
   - 注入 `RedisCacheManager`（Redis缓存管理器）

3. **ShiroFilterChainDefinition**: 过滤器链定义
   - 所有路径（`/**`）都使用 `jwt` 过滤器

4. **ShiroFilterFactoryBean**: 过滤器工厂
   - 注册自定义 `JwtFilter` 过滤器
   - 配置过滤器链映射

**安全机制**:
- 所有请求都经过JWT Token验证
- 未登录用户可以访问 `/login` 和 `/test`（JwtFilter中处理）
- 需要认证的接口使用 `@RequiresAuthentication` 注解

#### `config/CorsConfig.java` - 跨域配置
**作用**: 解决前后端分离的跨域问题

**配置内容**:
- 允许所有来源（`*`）
- 允许的HTTP方法：GET, HEAD, POST, PUT, DELETE, OPTIONS
- 允许携带凭证
- 预检请求缓存时间：3600秒
- 允许所有请求头

#### `config/MybatisPlusConfig.java` - MyBatis Plus配置
**作用**: 配置MyBatis Plus

**配置内容**:
1. **@MapperScan**: 扫描Mapper接口包路径
2. **PaginationInterceptor**: 分页拦截器
   - 支持分页查询功能

---

### 7. Shiro 安全模块

#### `shiro/AccountRealm.java` - 认证域
**作用**: Shiro的认证和授权核心类

**核心方法**:
1. `supports()`: 判断是否支持JwtToken类型的认证
2. `doGetAuthenticationInfo()`: 执行认证逻辑
   - 从JWT Token中解析用户ID
   - 查询用户信息
   - 检查用户状态（是否锁定）
   - 创建 `AccountProfile` 对象
   - 返回认证信息

3. `doGetAuthorizationInfo()`: 授权逻辑（目前未实现）

**认证流程**:
```
JWT Token → 解析用户ID → 查询用户 → 验证状态 → 返回认证信息
```

#### `shiro/JwtFilter.java` - JWT过滤器
**作用**: 拦截所有请求，验证JWT Token

**核心方法**:
1. `createToken()`: 从请求头获取JWT Token，创建JwtToken对象
2. `onAccessDenied()`: 访问被拒绝时的处理
   - 检查请求头是否有JWT Token
   - 验证Token是否有效（未过期）
   - 执行登录认证
3. `onLoginFailure()`: 登录失败时的处理
   - 返回JSON格式的错误信息
4. `preHandle()`: 预处理（处理OPTIONS预检请求）

**工作流程**:
```
请求 → 检查Authorization头 → 验证Token → 执行认证 → 放行/拒绝
```

#### `shiro/JwtToken.java` - JWT Token实现
**作用**: 实现Shiro的AuthenticationToken接口

**功能**: 封装JWT Token字符串，供Shiro认证使用

#### `shiro/AccountProfile.java` - 账户信息
**作用**: 存储当前登录用户的基本信息

**字段**:
- `id`: 用户ID
- `username`: 用户名
- `avatar`: 头像
- `email`: 邮箱

**用途**: 在Shiro会话中存储，通过 `ShiroUtil.getProfile()` 获取

---

### 8. Util 工具类

#### `util/JwtUtils.java` - JWT工具类
**作用**: 生成和解析JWT Token

**配置属性**（从application.yml读取）:
- `secret`: JWT密钥
- `expire`: Token过期时间（秒）
- `header`: Token在请求头中的字段名

**核心方法**:
1. `generateToken(userId)`: 生成JWT Token
   - 设置用户ID为Subject
   - 设置签发时间和过期时间
   - 使用HS512算法签名

2. `getClaimByToken(token)`: 解析Token获取Claims
   - 验证签名
   - 返回Claims对象（包含用户ID等信息）

3. `isTokenExpired(expiration)`: 判断Token是否过期

#### `util/ShiroUtil.java` - Shiro工具类
**作用**: 便捷获取当前登录用户信息

**方法**:
- `getProfile()`: 从Shiro SecurityUtils获取当前登录用户的AccountProfile

---

### 9. Common 公共模块

#### `common/lang/Result.java` - 统一响应结果
**作用**: 统一API响应格式

**字段**:
- `code`: 状态码（200=成功，非200=失败）
- `msg`: 消息
- `data`: 数据

**静态方法**:
- `succ(data)`: 成功响应（code=200）
- `succ(code, msg, data)`: 自定义成功响应
- `fail(msg)`: 失败响应（code=400）
- `fail(msg, data)`: 失败响应（带数据）
- `fail(code, msg, data)`: 自定义失败响应

#### `common/dto/LoginDto.java` - 登录DTO
**作用**: 接收登录请求参数

**字段**:
- `username`: 用户名（必填）
- `password`: 密码（必填）

**验证**: 使用 `@NotBlank` 注解验证

#### `common/exception/GlobalExceptionHandler.java` - 全局异常处理器
**作用**: 统一处理系统异常

**处理的异常类型**:
1. **ShiroException**: Shiro安全异常
   - 返回401状态码
   - 返回错误信息

2. **MethodArgumentNotValidException**: 参数验证异常
   - 返回400状态码
   - 返回第一个验证错误信息

3. **IllegalArgumentException**: 断言异常（Assert）
   - 返回400状态码
   - 返回错误信息

4. **RuntimeException**: 运行时异常
   - 返回400状态码
   - 返回错误信息

---

## 核心业务流程

### 1. 用户登录流程
```
1. 前端发送 POST /login {username, password}
2. AccountController 接收请求
3. 根据用户名查询用户
4. 验证密码（MD5加密对比）
5. JwtUtils 生成JWT Token
6. Token放入响应头 Authorization
7. 返回用户信息
8. 前端保存Token，后续请求携带在请求头
```

### 2. 请求认证流程
```
1. 前端请求携带 Authorization: Bearer {token}
2. JwtFilter 拦截请求
3. 从请求头提取Token
4. JwtUtils 验证Token有效性
5. 创建JwtToken对象
6. AccountRealm 执行认证
   - 解析Token获取用户ID
   - 查询用户信息
   - 验证用户状态
7. 认证成功，放行请求
8. Controller 处理业务逻辑
```

### 3. 博客编辑流程
```
1. 前端发送 POST /blog/edit {blog数据}
2. JwtFilter 验证Token
3. BlogController 接收请求
4. 判断是新建还是编辑
   - 新建：设置userId、created、status
   - 编辑：验证userId是否等于当前用户ID
5. 保存/更新博客
6. 返回结果
```

---

## 🎯 系统功能总结

### 已实现功能

1. **用户认证**
   - ✅ 用户登录（JWT Token）
   - ✅ 用户登出
   - ✅ Token验证
   - ✅ 密码MD5加密

2. **博客管理**
   - ✅ 博客列表（分页）
   - ✅ 博客详情
   - ✅ 创建博客
   - ✅ 编辑博客（权限控制）

3. **安全机制**
   - ✅ JWT Token认证
   - ✅ Shiro权限管理
   - ✅ Redis会话存储
   - ✅ 跨域支持

4. **数据访问**
   - ✅ MyBatis Plus ORM
   - ✅ 分页查询
   - ✅ 基础CRUD

### 功能特点

1. **前后端分离**: 使用JWT无状态认证，支持跨域
2. **权限控制**: 基于Shiro的细粒度权限控制
3. **会话管理**: Redis存储会话，支持分布式部署
4. **统一响应**: Result统一响应格式
5. **异常处理**: 全局异常处理器，统一错误响应

---

## 数据库设计

### users 表
- 存储用户基本信息
- 密码MD5加密存储
- status字段控制账户状态

### blog_articles 表
- 存储博客文章
- 关联用户ID（userId）
- status字段控制文章状态

---

## 配置文件说明

### application.yml
- 服务端口：8081
- JWT配置：密钥、过期时间、请求头字段名

### application-default.yml
- 开发环境配置
- MySQL连接：localhost:3306
- Redis连接：127.0.0.1:6379

### application-pro.yml
- 生产环境配置
- MySQL连接：mysql:3306（Docker容器名）
- Redis连接：redis:6379（Docker容器名）

---

## 🚀 项目亮点

1. **现代化架构**: Spring Boot + MyBatis Plus + Shiro
2. **无状态认证**: JWT Token，适合前后端分离和微服务
3. **分布式支持**: Redis会话管理，支持集群部署
4. **代码简洁**: 使用Lombok减少样板代码
5. **统一规范**: 统一的响应格式和异常处理

---

## 📝 待完善功能

1. **用户注册**: 目前只有登录，没有注册接口
2. **博客删除**: 只有编辑，没有删除接口
3. **博客搜索**: 没有搜索功能
4. **用户管理**: 用户信息管理功能不完整
5. **权限细化**: 目前只有认证，没有角色和权限管理
6. **文件上传**: 没有头像、图片上传功能

---

## 🔍 代码质量建议

1. **Service层**: 目前Service层只是简单继承，可以添加业务逻辑
2. **异常处理**: 可以添加更多业务异常类型
3. **日志记录**: 可以添加更详细的日志记录
4. **参数验证**: 可以添加更多参数验证规则
5. **单元测试**: 缺少单元测试和集成测试

---

## 📚 总结

这是一个结构清晰、技术栈现代化的博客系统后端项目。采用了主流的Spring Boot技术栈，使用JWT+Shiro实现安全认证，MyBatis Plus简化数据访问，Redis管理会话。代码结构遵循MVC分层架构，职责分明，易于维护和扩展。

项目适合作为学习Spring Boot、Shiro、JWT等技术的参考项目，也可以在此基础上扩展更多功能，如评论系统、标签管理、分类管理等。

