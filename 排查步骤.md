# Elasticsearch 数据同步失败排查步骤

## 🔍 当前问题

**现象**：索引为空（count: 0）
```bash
curl http://localhost:9200/blog_index/_count
# 返回: {"count":0,...}
```

## 📋 排查步骤

### 1. 检查应用启动日志

查看应用启动时是否有以下日志：

```
========================================
ElasticsearchInitService: 开始初始化...
========================================
查询到 X 篇已发布的博客
开始同步 X 篇博客到 Elasticsearch...
========================================
✓ 博客同步完成！
成功: X 篇
失败: 0 篇
========================================
```

**如果没有看到这些日志**：
- 应用可能还没有重启
- `ElasticsearchInitService` 可能没有被 Spring 扫描到
- `ApplicationReadyEvent` 可能没有触发

### 2. 检查数据库中是否有博客数据

```sql
-- 检查已发布的博客数量
SELECT COUNT(*) FROM blog_articles WHERE status = 0;

-- 查看所有博客
SELECT id, title, status FROM blog_articles;
```

**如果 count = 0**：
- 说明数据库中没有 `status = 0` 的博客
- 需要先创建一些博客数据

### 3. 检查 Elasticsearch 连接

查看启动日志中是否有：
```
✓ Elasticsearch 连接成功！
✓ Elasticsearch 索引 [blog_index] 创建成功！
```

**如果没有**：
- Elasticsearch 可能未启动
- 连接配置可能错误

### 4. 检查同步过程中的错误

查看日志中是否有：
```
❌ 同步博客到 Elasticsearch 失败 [ID: X]: ...
```

**如果有错误**：
- 查看完整的错误堆栈
- 可能是字段映射问题
- 可能是日期格式问题

### 5. 手动触发同步（推荐）

如果自动同步没有执行，可以使用手动同步接口：

```bash
# 需要先登录获取 token
curl -X POST http://localhost:8081/search/sync-all \
  -H "Authorization: YOUR_TOKEN" \
  -H "Content-Type: application/json"
```

或者在代码中直接调用：
```java
// 在 BlogController 中添加
@PostMapping("/search/sync-all")
public Result syncAllBlogs() {
    // 会同步所有 status = 0 的博客
}
```

### 6. 验证同步结果

```bash
# 检查索引中的文档数量
curl http://localhost:9200/blog_index/_count

# 查看索引中的文档
curl http://localhost:9200/blog_index/_search?pretty&size=5
```

## 🐛 常见问题

### 问题 1: 没有看到初始化日志

**原因**：
- 应用没有重启
- `ElasticsearchInitService` 没有被 Spring 扫描

**解决**：
1. 确认 `ElasticsearchInitService` 在正确的包路径下
2. 确认 `@Service` 注解存在
3. 重启应用

### 问题 2: 查询到 0 篇博客

**原因**：
- 数据库中没有 `status = 0` 的博客
- 博客的 status 字段值不是 0

**解决**：
1. 检查数据库中的博客数据
2. 确认 status 字段的值
3. 如果需要，修改查询条件

### 问题 3: 同步失败但无错误信息

**原因**：
- 异常被捕获但没有打印堆栈

**解决**：
- 查看最新代码，已添加 `e.printStackTrace()`
- 重启应用查看完整错误信息

### 问题 4: Elasticsearch 客户端为 null

**原因**：
- Elasticsearch 连接失败
- Bean 创建失败

**解决**：
- 检查 `ElasticsearchConfig` 的日志
- 确认 Elasticsearch 服务已启动

## ✅ 快速验证方法

1. **重启应用**，查看启动日志
2. **检查日志**，确认是否有同步相关的输出
3. **手动触发同步**（如果自动同步失败）
4. **验证结果**，使用 curl 检查索引中的文档数量

## 🎯 下一步

如果以上步骤都正常，但索引仍然为空，请提供：
1. 应用启动日志（特别是 ElasticsearchInitService 相关的）
2. 数据库中的博客数据（SELECT COUNT(*) FROM blog_articles WHERE status = 0）
3. 任何错误信息

